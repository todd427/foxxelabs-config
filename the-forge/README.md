# The Forge — moved

The Forge now lives in its own repo, deployed via Cloudflare Pages (git-connected):

**https://github.com/todd427/the-forge**

This directory previously held the prototype and a Vite build; both were moved to the dedicated repo on 2026-06-04 to match the Sionnach deploy pattern (one repo → Pages → push-to-deploy). Nothing deployable lives here anymore.

Design rationale and the archetype→class→doorway model are documented in the new repo's README.
